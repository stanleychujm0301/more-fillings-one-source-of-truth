{
  "name": "more-fillings-one-source-of-truth",
  "private": true,
  "version": "0.1.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:ui": "npm --prefix ui-new ci",
    "postinstall": "npm --prefix ui-new ci",
    "build": "npm --prefix ui-new run build",
    "dev": "npm --prefix ui-new run dev",
    "preview": "npm --prefix ui-new run preview"
  }
}
